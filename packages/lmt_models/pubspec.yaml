name: lmt_models
description: Shared data models and types for Last Mile Tracker.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.10.8

dependencies:
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
